import { MatchPassValidatorsDirective } from './match-pass-validators.directive';

describe('MatchPassValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPassValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
